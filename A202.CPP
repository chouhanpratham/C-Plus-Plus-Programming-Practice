#include<conio.h>
#include<iostream.h>
void main()
  //Here we using the cout and cin object 
  //We are using them to find the average of two numbers inputted by the user
{
  clrscr();
  int a,b,avg;
  cout<<"Enter two numbers to fing average \n";
  cin>>a>b;
  avg=(a+b)/2;
  cout<<"Average is "<<avg;
  getch();
}
